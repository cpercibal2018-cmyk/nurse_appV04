const {Client}=require('pg');(async()=>{const c=new Client({connectionString:'postgresql://aigh:12345678@localhost:5432/postgres'});await c.connect();await c.query('DROP DATABASE IF EXISTS nurseapp_unseeded_check WITH (FORCE)');await c.query('CREATE DATABASE nurseapp_unseeded_check');await c.end();console.log('created nurseapp_unseeded_check');})().catch(e=>{console.log('ERR',e.message);process.exit(1)});
